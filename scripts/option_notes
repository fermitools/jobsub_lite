Need to add:
--dropbox-server xxx
--debug
--drain?
--schedd (name(Pattern?) of schedd)

need to test separately:
   --dag dagnabbitfile
   --maxConcurrent

and tarfile tests
    --use-cvmfs-dropbox
    --tar_file_name
    --tarball-exclusion-file

params:
	[executable] [exe_arguments [exe_arguments ...] ]

flags:
	--generate-email-summary
	-n
	--devserver
	--nosubmit
	-Q
	--mail_on_error
	--mail_always
	--verbose
